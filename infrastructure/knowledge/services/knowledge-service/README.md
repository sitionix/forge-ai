# Knowledge Service

Local FastAPI service that reads the Forge AI service catalog, builds a SQLite file inventory, serves a transitional Jarvis context endpoint, and runs AI structural analysis. It does not implement embeddings, vector storage, or RAG.
