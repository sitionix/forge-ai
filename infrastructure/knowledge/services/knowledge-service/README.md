# Knowledge Service

Local FastAPI service that reads the Forge AI service catalog, builds a SQLite file inventory, and serves keyword/path search. It does not implement embeddings, vector storage, or RAG.
