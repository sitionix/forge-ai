# Knowledge Roadmap

V1 provides catalog-driven sources, local inventory, file metadata, and keyword/path search.

Not implemented in v1:

- embeddings
- vector DB
- semantic search
- RAG context API
- Jarvis integration
- prompt augmentation

Future vector metadata should include `sourceId`, service label, group, tags, contract refs, relative path, and content hash.
