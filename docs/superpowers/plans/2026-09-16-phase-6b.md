# Phase 6B Explicit Shared Session Group Implementation Plan

**Goal:** Explicitly share one Forge session and provider conversation among nodes in one context iteration.
**Architecture:** Extend Phase 6A policy and existing allocator/locks. Group-owned sessions have null node/agent owners; immutable NodeRun remains the invocation authority.
**Spec:** User-provided Phase 6B specification in this session.

## Constraints
Existing three modes keep their identities and behavior. No automatic conversion, parallel allocator, cross-run sharing or recovery fallback. Shared identity/lock: workflow + iteration + group + repository. Validate scopes/modes and immutable provider/model/effort before execution.

## Tasks
- [x] Domain/persistence: add mode and iterationScoped policy; V33 constraints and indexes; group-owned session field; extend allocator and Reset lock; tests for ownership, scopes, concurrency, retirement.
- [x] Validation/propagation: mixed mode rejection; validate RunNode snapshot models; parameterize existing iteration boundary/fan-in tests for both modes.
- [x] Provider: prove current instructions and schema on shared resume, using existing Codex adapter and protocol tests.
- [x] API/Console: propagate nullable ownership and group through Agent/Nexus; Builder explicit option and validation; Inspector group, iteration and actual invocation history.
- [x] Integration: A/B/A feedback, exit/reentry, differing instructions/schema; shared retry/recovery/reset/stop; first-allocation and reset races; historical migration compatibility.
- [ ] Acceptance: gated installed Codex two iterations and reset with recorded protocol; requested Agent/Nexus/Console verification; independent review; commit/PR and exact-head CI.

## Verification
`mvn -B -ntp -Dapi.version=1.40 -pl services/forge-agent/boot -am verify`
`mvn -B -ntp -pl services/forge-nexus/boot -am verify`
Console: `npm test -- --run`, `npm run typecheck`, `npm run build`.
`git diff --check`; gated shared live acceptance; final HEAD CI.

## Progress
Base origin/main 8a71e183, branch feature/SITIONIX-120 (maximum local/remote ID 119).
Console and Codex protocol work delegated under subagent-driven-development; backend and integration owned by primary agent. Work in clean existing checkout on new feature branch.

Local full verification: Agent 983 tests (7 gates skipped), Nexus 219, Console 513 + typecheck/build, migration suite 10, all pass. Independent review complete; shared Current label corrected. Real Codex gate runs sequentially due common project fixture cleanup.

Installed Codex shared gate PASS: CLI 0.154.0, two independent iterations and same-iteration reset replacement; exact session/thread evidence in docs/phase-6b-shared-session-acceptance.md. Remaining: commit/PR and final-head CI.
