{
  "payloadType": "ApiParameterPayload",
  "description": "One API operation parameter.",
  "fields": [
    { "name": "name", "type": "STRING", "required": true, "description": "Parameter name as it should appear in the API contract." },
    { "name": "in", "type": "STRING", "required": true, "description": "Parameter location, for example path, query, header, or cookie." },
    { "name": "required", "type": "BOOLEAN", "required": true, "description": "Whether this parameter is required by the API contract." },
    { "name": "type", "type": "STRING", "required": true, "description": "Parameter value type in contract terms." },
    { "name": "description", "type": "STRING", "required": true, "description": "Human-readable parameter purpose and constraints." }
  ]
}
