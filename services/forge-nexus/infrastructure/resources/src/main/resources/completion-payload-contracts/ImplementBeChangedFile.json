{
  "payloadType": "ImplementBeChangedFile",
  "description": "One backend production file changed by an implementation lane.",
  "fields": [
    { "name": "path", "type": "STRING", "required": true, "description": "Repository-relative file path." },
    { "name": "reason", "type": "STRING", "required": true, "description": "Why the file was changed and what behavior it owns." }
  ]
}
