{
  "payloadType": "ApiPayloadField",
  "description": "One field in an API request or response payload shape.",
  "fields": [
    { "name": "name", "type": "STRING", "required": true, "description": "Field name as it should appear in the API contract." },
    { "name": "required", "type": "BOOLEAN", "required": true, "description": "Whether this field is required in the payload." },
    { "name": "type", "type": "STRING", "required": true, "description": "Field type in contract terms." },
    { "name": "description", "type": "STRING", "required": true, "description": "Human-readable field purpose and constraints." }
  ]
}
